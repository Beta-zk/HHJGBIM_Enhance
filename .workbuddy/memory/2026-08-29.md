# 2026-08-29 工作日志

## 视图包解耦重构（view → 模块 + 内核 UI 层）

**背景**：经多轮架构研讨（user 提供 资料1.txt 方案研讨），拍板三项决策：
1. 弃用按属性分类（UI/功能/模块）推断面板挂载，改为显式 `panelEntry` 声明即挂载；
2. Shell 归入 `src/kernel/ui/`（不新增独立 shell 包）；
3. 排序码常量化：`PANEL_SORT_SETTINGS = 999`（设置置底），默认 `PANEL_SORT_DEFAULT = 1`。

**实施内容（commit 665b248）**：
- 新增 `src/kernel/ui/panelStore.ts`：面板入口注册表（register/unregister/sorted 稳定排序），
  EnhanceManager.activate/deactivate 同步注册/反注册，实现 Shell 零业务 import 的反向注入。
- `module.types.ts` 新增 `IPanelEntrySpec { label, icon?, sort?, action, text?, disabled? }`，
  挂载 `IEnhanceModule.panelEntry`（可选）。text/disabled 支持动态文案（如"数据拉取中..."）。
- `App.vue` 瘦身迁入 `src/kernel/ui/App.vue`：面板按钮 v-for 遍历 `panelStore.sorted` 渲染，
  仅保留动态插槽 `<component v-for uiStore.activeComponents>`；handleOpenReport 数据编排整体移除。
- 绩效视窗模块化：`src/modules/performance-report/`（store.ts + index.ts + view/PerformanceReport.vue + view/Chart.vue），
  props 注入 → reportStore 驱动；原 App.vue 的 handleOpenReport 逻辑迁入模块类 openReport()。
- 设置模块化：`src/modules/settings/`（store.ts + index.ts + view/SettingsPanel.vue），settingsStore 驱动显隐。
- main.ts 注册 performance-report 与 settings 两新模块。
- 旧 `src/view/*` 与 `src/kernel/uiStore.ts` 覆盖为 @deprecated 弃用壳（按 safe-delete 铁律不 rm，git 记录 M）。

**验证**：tsc --noEmit 通过；`rm -rf dist && vite build` 成功（137.82 kB user.js）。

**遗留**：`vite.config.ts` 有一处 icon 链接 main→master 的工作区改动，非本任务，已 unstage 留给用户。

## temp-fixes 插件拆分（commit 9cb7b2e）

- 新增 `src/modules/temp-fixes/index.ts`：集中维护宿主页面缺陷的临时修补样式（当前仅条码页表格滚动修复），
  默认启用、不声明 panelEntry（纯样式无需面板入口）、styleIds 登记 `temp-fix-barcode-table-scroll`。
- `barcode-print` 移除 TEMP_FIX 常量、styleIds、injectStyle，仅保留说明注释；职责纯净为条码查询编排。
- main.ts 注册 `tempFixesEnhance`。验证：tsc 通过，dist 重建成功（48 模块）。

## performance-report 数据加工时机修复（commit 349fcc2）

- **症状**：爬虫离线时打开绩效面板无数据（用户误判为降级逻辑失效）。
- **根因**：组件经 uiStore 动态沙箱常驻挂载后，onMounted 仅执行一次且早于数据就绪（reportData 仍为空），processData 在空数据上运行；此后 show=true 只切换 v-if 显隐、不重新挂载，processData 永不重跑 → 面板空白。
- **教训**：**uiStore 常驻挂载的组件（root v-if 控制显隐）不能依赖 onMounted 处理数据**，须用 watch(store 显隐开关) 在打开后重跑数据加工；「先拉数据 → 后开窗」时序依赖此保障。
- 降级逻辑本身在 FactoryService.fetchMonthlyOutput（useLocal=false 走宿主接口）未受重构影响。
- 修复：watch(reportStore.show) → nextTick → processData()。验证：tsc + build 通过。

## 工程大清洗（commit 7f79f4d + a47621c）

- **死文件删除**（7f79f4d）：`src/view/` 全部弃用壳（App.vue、components/PerformanceReport.vue、components/PerformanceReport/Chart.vue、components/Settings.vue）+ `src/kernel/uiStore.ts` 共 5 个 @deprecated 占位文件彻底删除（用户要求真实删除，非覆盖为弃用壳；逐文件 rm + git 提交可回滚）。
- **temp-fixes 去留决策**：用户确认**保留**插件（未来可能新增其他临时修复），main.ts 维持"注册处注释"的停用形态（import + `// tempFixesEnhance,` 均保留作为预留）。
- **注释 JSDoc 规范化**（a47621c，23 文件）：
  - 补齐公开 API 缺失的 @param/@returns：EnhanceManager(register/registerAll/start/dispose/isEnabled)、DomMaster(addClass/removeClass/toggleClass/setStyle/getElementById/querySelectorAll/findElementByText/clickElementByText/isUrlMatch/scrollIntoView/getCurrentUrl)、NetworkHook(registerHeaderSniffer/registerResponseInterceptor)。
  - 补全缺失 JSDoc 块：uiStore(register/unregister)、useDraggable(函数+UseDraggableOptions)、module.types(IResponseInterceptorSpec)、project-state/store(IStateItem)、main(mountVueUI)、temp-fixes(⚠️ 说明转 JSDoc)。
  - 精简冗余流程注释（barcode-print 悬浮框注释 2 行并 1 行）；保留全部关键业务单行注释（幂等/防坑/时序设计）。
  - 统一清理全部 .ts/.vue 行尾空格。
- **验证**：tsc --noEmit 通过；`rm -rf dist && vite build` 成功（47 模块，137.55 kB user.js）。
- **规范沉淀**：本工程注释基线 = JSDoc 标准（`@method/@function/@interface/@const` + `@param{类型} 含义` + `@returns{类型} 含义`），`//` 单行注释仅保留解释"为什么"的关键防坑注释；结构化 section 分隔注释（`// ====================`）保留。

## project-state 拦截器摘除（commit 84a28b4）

**背景**：研判确认 project-state 的 `INTERCEPTOR_PROJECT_FILTER`（拦截 WAREHOUSE_DATA_STATS + GET_RAW_WH_SUMMARY_LIST 提取状态）为**冗余且有害的历史遗留**：
- 与 material-inventory 同抢 GET_RAW_WH_SUMMARY_LIST，NetworkHook 的 `Array.find` 只命中先注册者 → material-inventory 的 transform/onResponse **从未执行**（功能失效 BUG）。
- 每次响应触发 `populateFilterStates` 会 `activeStates.clear()` → **重置用户筛选勾选**（隐性 BUG）。
- 状态提取能力与 DOM 扫描路径（scanAndApply）重叠度 >90%，差异仅为"响应到达即激活"（早 150ms，无感知收益）。

**改动**：删除 interceptors 声明 + `extractFilterStates()` + 失引的 `API_URLS` import，共 31 行删除、零新增。功能收敛为单主路径：字典预热 → MutationObserver → scanAndApply（挂圆点/筛选/页脚重算）。
**副作用（正向）**：material-inventory 在仓汇总页自动恢复排序过滤与宽度修正；用户勾选不再被响应重置。
**验证**：tsc 通过；`rm -rf dist && vite build` 成功（47 模块，136.73 kB）。
**待真机验证**：项目列表页 + 原材料仓汇总页确认圆点挂载、筛选联动、仓汇总排序过滤三项功能。

## project-state 筛选字块默认激活配置路由名单化（commit cf86ed6）

- 原 `populateFilterStates` 无条件默认排除「完工未验收/完工已验收」，现改为**路由名单驱动**：
  - 新增 `DEFAULT_EXCLUDED_ROUTES = ['/produce/pro/project-inventory']`（子串匹配，与 BLACKLIST 同风格）
  - 新增 `DEFAULT_EXCLUDED_STATES = ['完工未验收', '完工已验收']` 独立常量
  - 抽离 `shouldApplyDefaultExclusion()`：命中名单 → 执行默认排除；名单外 → 激活全部字块
- 验证：tsc 通过；`rm -rf dist && vite build` 成功（47 模块，136.99 kB）。
