/**
 * @const RawInventoryHost
 * @description 原材料仓库存页宿主页面契约：集中布局容器选择器（宽度修正目标）。
 */
export const RawInventoryHost = {
    /** 目标容器选择器（width/maxWidth 均修正为 100% 以填充父容器） */
    CONTAINER_SELECTOR: '#app .app-wrapper .app-main .container.abs100'
};
